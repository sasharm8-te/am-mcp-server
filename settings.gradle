pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url teRepoUrl
            credentials {
                username teRepoUser
                password teRepoPassword
            }
        }
    }
}

rootProject.name = 'am-mcp-server'
